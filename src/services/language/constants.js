export const EN_LOCALE = "en";
export const VI_LOCALE = "vi";

// Action names
const BASE_NAME = "LANGUAGE";
export const CHANGE_LOCALE = [BASE_NAME, "CHANGE_LOCALE"].join("/");

// Default values
export const DEFAULT_LOCALE = EN_LOCALE;
